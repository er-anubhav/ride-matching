name: shared
description: "Shared design tokens, theme, and common UI widgets for UrbanPulse."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.6.0
  lucide_icons: ^0.257.0
  shared_preferences: ^2.5.5
  web_socket_channel: ^3.0.1

